var Q = require("q");
var FS = require("./q-fs");
FS.listTree("", function (name, stat) {
    if (["node_modules", ".git"].indexOf(name) >= 0)
        return null;
    return Q.ref(true);
}).forEach(function (name) {
    console.log(name);
});
